name: ui_helper
description: "UI helper wrapper."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  flutter_svg: ^2.3.0
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

